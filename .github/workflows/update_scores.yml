name: Aggiorna Ascension Mythic+ Score

on:
  schedule:
    # Esegue in automatico ogni 6 ore
    - cron: '0 */6 * * *'
  workflow_dispatch: # Permette di avviarlo a mano con un clic

jobs:
  update-db:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installa dipendenze
        run: |
          pip install playwright
          playwright install chromium

      - name: Esegui scraping Leaderboard
        run: python scraper.py

      - name: Salva e fai il commit di ScoreDB.lua
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add ScoreDB.lua
          git commit -m "Auto-update ScoreDB.lua [skip ci]" || exit 0
          git push
